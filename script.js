// script.js
window.addEventListener('DOMContentLoaded', () => {
    // Scroll animations
    const sections = document.querySelectorAll('section');
    const options = {
        root: null,
        threshold: 0.1
    };
    ... (TRUNCATED FOR DEMO PURPOSES - will include full code in actual output)
});